<%- include('../partials/header', { titrePage: 'Ventes' }) %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h3 class="mb-0">Ventes de tissus cousus</h3>
  <a href="/ventes/nouveau" class="btn btn-dark">+ Nouvelle vente</a>
</div>

<div class="card">
  <div class="table-responsive">
    <table class="table table-hover mb-0 align-middle">
      <thead>
        <tr>
          <th>Date</th>
          <th>Client</th>
          <th>Qualité</th>
          <th>Modèle</th>
          <th class="text-end">Prix unit. vente</th>
          <th class="text-end">Quantité</th>
          <th class="text-end">Total</th>
          <th class="text-end">Payé</th>
          <th class="text-end">Solde dû</th>
          <th class="text-end">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if (lignes.length === 0) { %>
          <tr><td colspan="10" class="text-center text-muted py-4">Aucune vente enregistrée pour le moment.</td></tr>
        <% } %>
        <% lignes.forEach(item => { const solde = (item.montant_total - item.montant_paye); %>
        <tr>
          <td><%= item.date_vente %></td>
          <td><%= item.client_nom %></td>
          <td><%= item.qualite %></td>
          <td><%= item.modele %></td>
          <td class="text-end"><%= item.prix_unitaire_vente.toLocaleString('fr-FR') %></td>
          <td class="text-end"><%= item.quantite %></td>
          <td class="text-end"><%= item.montant_total.toLocaleString('fr-FR') %></td>
          <td class="text-end"><%= item.montant_paye.toLocaleString('fr-FR') %></td>
          <td class="text-end <%= solde > 0 ? 'solde-positif' : '' %>"><%= solde.toLocaleString('fr-FR') %></td>
          <td class="text-end">
            <a href="/ventes/<%= item.id %>/modifier" class="btn btn-sm btn-outline-secondary">Modifier</a>
            <form action="/ventes/<%= item.id %>/supprimer" method="POST" class="d-inline" onsubmit="return confirm('Supprimer cette vente ?');">
              <button type="submit" class="btn btn-sm btn-outline-danger">Supprimer</button>
            </form>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<%- include('../partials/footer') %>
